[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "adsb-collector"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["redis", "pyModeS"]

[tool.setuptools]
package-dir = {"" = "src"}
